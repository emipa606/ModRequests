using System;
using RimWorld;
using Verse;

[DefOf]
public static class RCSSDefOf
{
    //the only reason this is here is so that I can assign zope as a "allowedForJoy" thing in that one harmony patch
    public static ThingDef RC2_Zope;
}
