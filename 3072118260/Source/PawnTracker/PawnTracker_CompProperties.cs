using Verse; 

namespace PawnTrackerMain
{
    public class CompProperties_PawnTracker : CompProperties
    {
        public CompProperties_PawnTracker()
        {
            compClass = typeof (PawnTrackerComp);
        }
    }
}
