using System;
using RimWorld;
using Verse;

namespace davcin {
    public class SortBySkillSocial : TransferableComparer {
        public override int Compare(Transferable lhs, Transferable rhs) {
            return davcin.SortBy.CompareSkill(SkillDefOf.Social, ref lhs, ref rhs);
        }
    }
}
